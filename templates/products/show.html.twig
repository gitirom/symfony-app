{% extends 'base.html.twig' %}

{% block title %}Product Details{% endblock %}

{% block body %}
    <h1>{{ product.name }}</h1>
    <p>Price: ${{ product.price }}</p>
    <a href="{{ path('products_index') }}">Back to products list</a>
{% endblock %}